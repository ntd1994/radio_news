class AppBarViewSize {
  const AppBarViewSize._();

  static const logoHeight = 64.0;
  static const logoWidth = 64.0;
  static const searchWidth = 20.0;
  static const searchHeight = 20.0;
  static const toolBarHeight = 80.0;
}

class RecordingListViewSize {
  const RecordingListViewSize._();

  static const lineWidth = 2.0;
  static const header = 0.10; // 10% viewport.
  static const recording = 0.50; // 50% viewport.
  static const list = 0.35; //35% viewport.
  static const footer = 0.05; // 5% viewport
}

class HeaderViewSize {
  const HeaderViewSize._();

  static const userConnectionWidth = 20.0;
  static const userConnectionHeight = 20.0;
}
